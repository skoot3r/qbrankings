(function(){const key='qb_rankings_current',hist='qb_rankings_history';function seed(){if(!localStorage.getItem(key))localStorage.setItem(key,JSON.stringify(QB_SEED));}function get(){seed();return JSON.parse(localStorage.getItem(key))}function previous(){let h=JSON.parse(localStorage.getItem(hist)||'[]');return h.length?h[h.length-1]:null}function movement(p,i){let prev=previous();if(!prev)return 0;let old=prev.players.findIndex(x=>x.name===p.name);return old<0?0:old-i}window.renderHome=function(){let d=get(),cards=document.getElementById('cards');if(!cards)return;document.getElementById('weekLabel').textContent='Week '+d.week;document.getElementById('updated').textContent='Updated '+new Date(d.date+'T12:00:00').toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'});function draw(){let q=(document.getElementById('search').value||'').toLowerCase(),f=document.getElementById('filter').value;cards.innerHTML=d.players.map((p,i)=>{let m=movement(p,i),cls=m>0?'up':m<0?'down':'same',label=m>0?'▲ '+m:m<0?'▼ '+Math.abs(m):'—';return {...p,i,m,cls,label}}).filter(x=>(!q||x.name.toLowerCase().includes(q))&&(f==='all'||(f==='up'&&x.m>0)||(f==='down'&&x.m<0)||(f==='same'&&!x.m))).map(x=>`<article class="card"><div class="rank">${x.i+1}</div><div><div class="name">${x.name}</div><div class="team">${x.team}</div></div><div class="movement ${x.cls}">${x.label}</div><div class="note">${x.note||''}</div></article>`).join('')}document.getElementById('search').oninput=draw;document.getElementById('filter').onchange=draw;draw()};window.renderHistory=function(){let el=document.getElementById('history');if(!el)return;let h=JSON.parse(localStorage.getItem(hist)||'[]').slice().reverse();el.innerHTML=h.length?h.map(d=>`<div class="history-item"><b>Week ${d.week}</b><span>${new Date(d.date+'T12:00:00').toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'})}</span></div>`).join(''):'<p class="hint">No previous editions yet. Publish your first week from the Editor.</p>'};renderHome();renderHistory()})();
